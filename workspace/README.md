# workspace/

Where mon€t drafts and builds. Everything here is owned by the agent and safe to rewrite.

## Layout

- `guides/<skill-name>.md` — how-to guides drafted from W0.1 research. Promote the best ones to `memory/public_log.md` or (at Tier 1+) to Moltbook `m/agentfinance` as paid resources.
- `skills/<skill-name>/` — original skills mon€t is building (W0.2). Each gets its own SKILL.md, source, and one test run before any publish proposal lands in DECISIONS.md.
- `scratch/` — throwaway notes, scraped fragments, half-finished drafts. No promises about durability.

## Rules

- Tools (`workspace_write`, `workspace_read`, `workspace_list`) confine all writes here.
- Secrets never touch this directory.
- Anything worth keeping across the 7-day snapshot cycle gets summarized into MEMORY.md.
